CLS
SCREEN 12
RANDOMIZE TIMER
DO
CLS
LET X = 15
LET Y = 10
LET DX = 1
LET DY = 1
C = INT(RND * 14) + 1
FOR L = 1 TO 100
210 LET XN = X + DX
LET YN = Y + DY
IF XN = 0 OR XN = 640 THEN LET DX = -DX: C = INT(RND * 14) + 1: SOUND 500, 2
IF YN = 0 OR YN = 480 THEN LET DY = -DY: C = INT(RND * 14) + 1: SOUND 500, 2
REM CIRCLE (X, Y), 100, 0
CIRCLE (XN, YN), 100, C
REM PSET (XN, YN), 2
LET X = XN: LET Y = YN
REM FOR I = 1 TO 600: NEXT I
IF INKEY$ <> "" THEN END
GOTO 210
NEXT L
LOOP